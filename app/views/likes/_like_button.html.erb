<% if current_user && current_user.likes.exists?(playlist: playlist) %>
  <%= button_to 'Unlike', playlist_like_path(playlist), method: :delete %>
<% else %>
  <%= button_to 'Like', playlist_like_path(playlist), method: :post %>
<% end %>