<% if @user.followers.include?(current_user) %>
    <%= button_to 'Unfollow', follows_destroy_path(followee: @user, follower: current_user), method: :delete %>
<% else %>
    <%= button_to 'Follow', follows_create_path(followee: @user.id, follower: current_user.id), method: :post %>
<% end %>