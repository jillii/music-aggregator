<div class="tag">
    <%= link_to tag.label, playlists_path(tag: tag.label) %>

    <% if is_user %>
        <%= button_to "", {controller: :tags, action: :destroy, id: tag.id, playlist_id: @playlist.id}, method: :delete, class: "btn-trash" %>
    <% end %>
</div>